# Dotfiles Notes

## Future Considerations

- **nvim-autopairs** (`windwp/nvim-autopairs`) — auto-close brackets, quotes, and parens. Integrates with nvim-cmp. Try it for a week before committing. May conflict with muscle memory and Rust lifetime annotations.
- **nvim-surround** (`kylechui/nvim-surround`) — Lua rewrite of vim-surround with Treesitter support. `cs"'`, `ds(`, `ysiw"`, etc. Previously used vim-surround but removed it.
